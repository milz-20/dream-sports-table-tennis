# Payment API Configuration
# After deploying the CDK stack, copy the PaymentApiUrl from the outputs
# and set it here (without trailing slash)
NEXT_PUBLIC_PAYMENT_API_URL=https://your-api-id.execute-api.ap-south-1.amazonaws.com/prod

# Google OAuth Configuration (for Sign In with Google)
# Get this from: https://console.cloud.google.com/apis/credentials
NEXT_PUBLIC_GOOGLE_CLIENT_ID=your_google_client_id_here.apps.googleusercontent.com

# AWS SNS Configuration (for SMS order notifications)
# Your phone number to receive SMS notifications (include country code)
YOUR_PHONE_NUMBER=+919325173787

# AWS Region (default: ap-south-1 for India)
AWS_REGION=ap-south-1

# AWS Credentials (automatically picked up from Amplify environment)
# No need to set these manually when deployed to Amplify
