# Payment API Configuration
# After deploying the CDK stack, copy the PaymentApiUrl from the outputs
# and set it here (without trailing slash)
NEXT_PUBLIC_PAYMENT_API_URL=https://your-api-id.execute-api.ap-south-1.amazonaws.com/prod

# NextAuth Configuration
NEXTAUTH_URL=http://localhost:3000
NEXTAUTH_SECRET=your-secret-key-here-generate-with-node-generate-secret.js

# Google OAuth (Get from Google Cloud Console - https://console.cloud.google.com/)
GOOGLE_CLIENT_ID=your-google-client-id
GOOGLE_CLIENT_SECRET=your-google-client-secret

# AWS Configuration
AWS_REGION=ap-south-1
DYNAMODB_TABLE_NAME=TableTennisStack-TableTennisTable
